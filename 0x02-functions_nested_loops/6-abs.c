#include "main.h"
#include <stdlib.h>

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @a: returns an abs
 *
 * Return: return abs
 */

int _abs(int a)
{
	int h = abs(a);

	return (h);
}
