#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - a program that prints single digits of base ten
 *
 * Return: return 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
